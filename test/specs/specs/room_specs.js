const assert = require('assert');
const Room=require('../models/room.js');




describe('Room', function(){
    let room1;
    
    this.beforeEach(function(){
        room1=new Room(10,painted);
    });

    it('should have an area in squremeters')
    it('should be able to check whether the room is not painted')
    it('should be able to check whether the room is able to be painted')

})