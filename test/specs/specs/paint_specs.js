const assert = require('assert');
const Paint=require('../models/paint.js')

//paint is in can; one can==one litre;
describe('Paint',function(){
let paint1;

//     this.beforeEach(function(){
// paint1=new Paint(---properties--);
//     });
it('should have a number of litres of pain in cans');
it('should be able to check if the paintcan is empty');
it('should be able to empty the paintcan');



})