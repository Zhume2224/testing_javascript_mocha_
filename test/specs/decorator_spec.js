const assert = require('assert');
const Decorator=require('../models/decorator.js')
const Room=require('../models/room.js')
const Paint=require('../models/paint.js')



describe('Decorator',function(){
    let deco1;

    // this.beforeEach('Decorator',function(){
    //     deco1=new Decorator(---objects---)

    // });

   it('should start with an empty paint stock')
   it('should be able to add cans of paint to paint stock')
   it('should be able to calculate total litres of paint in stock')
   it('should be able to check wether paint in stock in enough to paint the whole area in a room')
   it('should be able to paint the room if there is enough paint in stock')




    


})